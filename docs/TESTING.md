# Testing

```bash
cd backend && python3 -m pytest -q
cd frontend && npm test -- --run && npm run build
```

Complete the manual E2E sequence documented in VALIDATION.md before tagging a release candidate.
