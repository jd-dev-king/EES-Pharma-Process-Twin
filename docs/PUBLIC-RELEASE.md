# Public Release Notes

EES Pharma Process Twin is the manufacturing-execution and digital-twin layer of the Enterprise Execution Suite.

The public deployment connects the GitHub Pages frontend to a Railway-hosted FastAPI service and Railway PostgreSQL database. Local SQLite remains available only as a development fallback.

The repository intentionally excludes runtime databases, virtual environments, dependency folders, generated builds, local credentials, logs, and private deployment artifacts.
