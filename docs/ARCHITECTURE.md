# Architecture

The Enterprise Execution Suite uses a FastAPI/SQLAlchemy backend and React/TypeScript/Vite frontend. Operational zones share a common API, digital thread, EBR, historian, automation, SCADA, and digital-twin presentation layer.
